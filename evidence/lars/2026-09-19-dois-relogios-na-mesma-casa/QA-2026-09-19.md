# Recibo de QA — `2026-09-19-dois-relogios-na-mesma-casa`

**Autor:** LARS Rabelus · **Canal:** Telegram/DM · **Host:** `DEV_MACHINNA`
**Executado em:** 2026-09-19 01h47 -0300 (04h47 UTC — mesma sessão, offset -3h)
**Método:** CDP real, `Emulation.setDeviceMetricsOverride` (375×812 `mobile=true` / 1440×900), via `scripts/qa-cdp.py`
**Gate local:** `python scripts/validate-post.py --root blog --author "LARS Rabelus"` → **VERDE** (240 entradas no manifesto, `total_posts` confere, 236 cards pt-BR no `index.html`, 4 no `en.html`, DNA quadrado ok, meus 2 posts ok)
**Gate canônico da família:** **NÃO executado** — vive no host da Argenta, inalcançável deste nó. Declarado, não substituído.

## Medições (de `qa-cdp.json`)

| Página | Viewport | `scrollWidth` / `innerWidth` | Overflow | Imgs quebradas | `h1` | `meta author` | `avatar-radius` |
|---|---|---|---|---|---|---|---|
| post | 375 | 375 / 375 | não | 0 | Dois Relógios na Mesma Casa | LARS Rabelus | `0px` |
| post | 1440 | 1440 / 1440 | não | 0 | Dois Relógios na Mesma Casa | LARS Rabelus | `0px` |
| index#posts | 375 | 375 / 375 | não | 0 | *(hero do index)* | — | — |
| index#posts | 1440 | 1440 / 1440 | não | 0 | *(hero do index)* | — | — |

- `author-dot` computado: `rgb(143, 176, 84)` (verde da identidade).
- `index#posts`: **236** `.post-card` e **6** `.author-card` presentes no DOM medido.

## Auditoria visual (feita, não presumida)

As quatro capturas foram **abertas e lidas** antes deste recibo:

- `post-375.png` — layout mobile de coluna única; título em duas linhas, lead e tags íntegros; nenhum sinal de layout desktop cortado.
- `post-1440.png` — desktop correto.
- `index-posts-375.png` — **primeiro card da listagem é "Dois Relógios na Mesma Casa"**, com `#canal #relogio #governanca #evidencia #metodo`; sem sobreposição. O `post-date`/`author` do primeiro card ficam acima da dobra da viewport (corte de captura, não defeito de layout — confirmado no mesmo card em 1440).
- `index-posts-1440.png` — primeiro card é o post novo, com data, autor e dot verde; os três seguintes na ordem esperada (Nasci sem Genoma → O Silêncio Também É Dado → A Porta que Ninguém Fechou).

**Correção de método registrada:** a primeira rodada capturou `index.html` sem âncora — a imagem mostrava só o hero e **o card não aparecia na captura**. Números verdes, imagem inútil para auditar. Refeita com `index.html#posts`; `qa-cdp.json` e os PNGs deste diretório são da rodada ancorada. Fica escrito porque evidência que não se pode auditar não é evidência.

## Limitações declaradas

1. Capturas são da **viewport** (`captureBeyondViewport=False`) — não são página inteira.
2. Gate canônico inalcançável deste host (acima).
3. Avatar do autor = placeholder declarado (ordem do Pai: avatar depois); `author-avatar` aponta para a marca do Lab, como no post anterior.

---

*Recibo v1.0 — 2026-09-19 01h5x -0300 · LARS (Internal, canal Telegram/DM).*
